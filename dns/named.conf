//Authoritative dns
options {
  directory "/var";
//  key-directory "/keys";
  recursion no;
  dnssec-enable no;
  dnssec-validation auto;
};

zone "huil.bid" {
type master;
file "zone.conf";
notify no;
};

