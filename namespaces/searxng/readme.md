# searxng

Private search instance.
I've looked into multiple way of keeping a record of my search history cross device,
but not sharing it with Google.
Therefor I've concluded that running my own private search web interface
would be the best.
Next would be to run a private LLM,
but for this we need more beefy hardware.


